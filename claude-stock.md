# Stock：実装時の共通ルール

2026-09-20。ユーザー方針。継続して適用するルールだけを置く。

- Codexが設計と変更内容を具体化し、ClaudeはFlowの手順を実装する。確定事項を再設計しない。
- カード効果・数値は固定。調整は指示されたカードの追加・デッキ構成で行う。個別に承認された効果変更はその指示に従う。
- Flowで指定したファイルの必要箇所だけ読む。全体探索、古い引き継ぎの通読、過去作業のやり直しをしない。
- 他者の未コミット変更、進行中セーブを保持する。旧BAKA BREAKERのフォルダと公開サイトは変更しない。
- 明示依頼がない限り公開・コミット・ホスティング変更をしない。

## 確認の分担

**人間が実際に遊んで、楽しさ・バランス・使用感を確認する。AIは変更箇所の最低限の挙動確認で終える。**

- Flowに確認方法と期待値を1つの短い手順として書く。既存テストで確認できればそれを絞って1回実行する。
- 小さな配分・表示変更のために新しいテスト基盤や大量のテストを作らない。変更に伴い古くなる既存テストの期待値は最小修正する。
- 全テスト・毎回のビルド・全画面サイズ巡回・大量シード試遊・性能比較は既定で行わない。
- 保存形式、認証、複雑な効果処理などを変更する場合は、Codexがその変更に必要な最小チェックをFlowへ明記する。関係ないチェックへ広げない。
- 成功後の再実行は不要。失敗した場合だけ原因箇所を読み、今回変更由来なら最小修正して該当確認を再実行する。無関係な不具合は短く記録する。
- ログ全文を会話に流さず、成功/失敗と必要なエラーだけ報告する。確認していないことを確認済みと書かない。

## 完了の扱い

Flowの変更と最小確認が済めば終了。人間の試遊を待つためにAI側で追加作業を続けない。

結果は `docs/claude-result.md` 冒頭へ「変更」「確認」「人間が遊ぶ際の確認点」を数行で追記する。既存結果は保持する。

Flowと実コードが大きく違う場合は勝手に探索範囲を広げず、差異を短く返す。必須のAGENTS指示も適用する。
