# デッキ拡張ロードマップ v42

対象：BAKA FANTASY。2026-09-19。状態：ユーザー承認済みの方針。実装・ブラウザ検証の完了をこの文書だけから主張しない。段1の構造変更のみ着手し、段2以降は未着手。

## 最新の合意：テーマとデッキの数え方

ユーザー向けの呼称は「核」ではなく「テーマ」。以下の旧実装説明にある核LEGEND／核GODは、テーマの主役LEGEND／GODを指す。コード内の `CORES` / `coreId` は現状維持。

各色に2テーマを用意し、単色1テーマ・単色2テーマ・多色2テーマのデッキを作る。組み合わせの順序は区別しない。3色なら単独6＋同色ペア3＋異色ペア12＝**21通り**。4色なら8＋4＋24＝**36通り**。一般には2nテーマから1つまたは2つ選ぶので `2n + C(2n, 2) = n(2n+1)`。これは選択肢の計画数であり、実装済み・有効性検証済みのデッキ数ではない。

段2でまず各色2テーマの単独デッキを作り、同色／異色2テーマの選択・初手・報酬契約は後続で設計する。多色専用の新テーマは必須ではなく、既存テーマの組み合わせを基本とする。段3の追加6枚は確定要件ではない。テーマ数に対する全ペアは二次的に増えるため、全ペアへ専用カードを追加するとカード制作も線形には収まらない。

## 0. 前提：インフレ軸

BAKA FANTASY は**インフレを楽しむゲーム**であり、各色でインフレさせる軸が異なる。

| 色 | インフレ軸 |
| --- | --- |
| 青 chain | 連鎖 |
| 赤 blood | HP消費（HP支払累計） |
| 緑 grave | エナジー |

数字が青天井に伸びること自体が設計意図である。インフレを抑制する方向（退場・回数制限・上限）でバランスを取らない。遊び応えは敵HPやギミック側で調整する。これは `feedback-design-v2.md` および青の退場全撤廃 `blue-recycling-v2.6.md` の方針を継承する。

## 1. ロードマップ

| 段 | 内容 | 新規カード | デッキ数 |
| --- | --- | ---: | ---: |
| 1 | 各色が1つ目の核を持つ。構造の導入 | **0枚** | 3 |
| 2 | 各色が2つ目の核（サブ格）を持つ。例：青＝「連鎖」と「連鎖×ドロー」 | 6枚 | 6 |
| 3 | 同色／多色の2テーマを組み合わせる | 専用追加は必須でない。補助札は別途判断 | 21（計画） |
| 4 | 色そのものを追加（4色・各2テーマ） | 1色分。組み合わせ専用札は別途判断 | 36（計画） |

デッキ候補はテーマ同士の組み合わせで増える。テーマごとの主役カードを再利用し、全組み合わせに専用札を要求しないことで、制作枚数を抑えて選択肢を増やす。無駄なバニラカードは追加しない。

## 2. 核カードの二段構成

各デッキは1枚の**核カード**で識別する。核は LEGEND と GOD の二段で構成する。

- **核LEGEND**：冒険開始時に配布し、各戦闘の初手に必ず入る。デッキの勝ち筋を決める。
- **核GOD**：核LEGENDのステータスを壊した上位版。報酬で確定1回だけ提示する。
- **汎用GOD**：核以外の報酬GOD。ランダムに提示する。各色に3枚（非ATTACK・資源系）既存。

核は抽選を通らないため、カードプールが増えても核へ到達する確率は下がらない。汎用GODだけが抽選対象になる。

## 3. 段1：各色の1つ目の核は既存カードで揃っている

LEGEND→GOD の梯子は、同じ入力軸・同じ ATTACK・倍率3〜8倍という同形で3色とも既に実装済みである。名称が与えられていなかっただけで、新規カードは不要。

| 色 | 核LEGEND | 核GOD | 入力軸 |
| --- | --- | --- | --- |
| 青 | 共鳴砲・環奏 c4（連鎖×140） | 世界樹の一弦大弓 c9（連鎖×1,200） | 連鎖 |
| 赤 | 帰還を誓う剣 c6（HP支払累計×12） | 還命の太陽大剣 c9（HP支払累計×60） | HP支払累計 |
| 緑 | 道標の巨人 c60（捨て累計×300＋E²×3） | 全魔力の神代巨兵 c100（捨て累計×1,000＋E²×8） | 捨て累計＋エナジー |

段1で行うのはカード追加ではなく構造変更である。

1. 核LEGENDを冒険開始時に配布し、各戦闘の初手に固定する。
2. 報酬を「核GOD確定1回＋汎用GODランダム」に変更する。
3. **色（パレット）と核（デッキ）を分離する。**

3番目を段1で行う理由は、多色（段3）では「色は2つ、核は1つ」になり、現在の `archetype` が兼務している4つの役割が必ず破綻するためである。現在 `archetype` はデッキ識別・背景/カード枠のパレット・報酬の自色判定・セーブ検証を同時に担っている。分離を後回しにするとセーブ移行を伴う。

## 4. 実測による根拠

`lib/game/engine.ts` を直接駆動したヘッドレス試遊による。素朴な方策（非アタックを安い順に全て使ってから殴る）で、1ターン68手のような人間ではない打ち方を含む。**完走率を人間の勝率とみなさない。** 軸ごとの到達値と敗因の内訳は方策によらない構造として扱う。

各色のインフレ軸の到達値（イージー40試行・中央値）：

| デッキ | 連鎖 | HP支払累計 | エナジー | 獲得倍率 | 最大単発 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 青 chain | 7.0万 | 0 | 3,235 | 1 | 9,979万 |
| 赤 blood | 35.1兆 | 4.7万 | 364 | 1 | 1億 |
| 緑 grave | 129 | 0 | 1.8億 | 629万倍 | 2,999万 |

難易度別の完走率（40試行）：

| デッキ | easy | normal | hard |
| --- | ---: | ---: | ---: |
| 青 chain | 40/40 | 25/40 | 13/40 |
| 赤 blood | 32/40 | 25/40 | 24/40 |
| 緑 grave | 18/40 | 12/40 | 5/40 |

青のノーマルでの敗北は全件が3アウトで、HP切れは0件。第3戦（`理を織り替える古竜`／コスト+1）に集中し、SKILLが+1されたターンのプレイ数は平均9.4手、他の種別なら21.7〜23.7手だった。青は24枚中13枚がSKILLで0コスト札2枚も両方SKILLのため、このギミックが青を狙い撃ちする形になっている。

イージーには3アウトも時間制限も無いため、難易度に依存する効果は核の設計に使えない。核は勝率を上げるものではなく、**勝ち方の形を変えるもの**として設計する。

## 4.5 段1の実装と検証

`coreVersion: 42` と `coreId` を `GameState` に追加し、`CORES` を `lib/game/engine.ts` に定義した（`BRIDGES` と同じ層に置き、生成物である `lib/game/cards.ts` は触らない）。核LEGENDは `beginBattle` で初手へ確保し、報酬の第1枠は核GODを未所持なら確定提示、以降は自色の汎用GODから抽選する。既存セーブは `coreVersion` を持たないため、従来の報酬フローのまま継続する。`coreVersion: 41` を渡せば新規冒険でも旧フローを再現でき、v3のGOD配布契約は `tests/god-expansion-v3.test.ts` でその形に固定した。

契約の確認（各40試行、素朴な方策）：核LEGENDは**全戦闘の初手に100%入り**（例：青イージーで200/200戦闘）、核GODは**全ケースで報酬1回目に提示**された。

完走数の前後比較（40試行）：

| デッキ | 難易度 | 核なし(v41) | 核あり(v42) |
| --- | --- | ---: | ---: |
| 青 chain | easy / normal / hard | 40 / 31 / 23 | 40 / 30 / 25 |
| 赤 blood | easy / normal / hard | 32 / 25 / 24 | 31 / 28 / 23 |
| 緑 grave | easy / normal / hard | 18 / 12 / 5 | **24 / 21 / 9** |

青と赤はほぼ不変で、段1が既存の手触りを変えていないことを示す。緑だけが大きく改善したのは、核LEGENDの `道標の巨人`（c60）が毎戦初手へ入り、重量攻撃が引けないまま終わる展開が無くなったため。これは「§5 緑の打点変換」の根本解決ではなく、引きの偏りが減っただけである。緑の軸そのものの扱いは未着手のまま残る。

テスト：`tests/deck-core-v42.test.ts` を追加（8件）。`npm test` は unit 409（407 pass / 2 skip）・auth 6・storage 7・SSR 4 が通過し、ビルドと同期チェックも成功した。**ブラウザでの目視確認は未実施。** 合言葉認証が必要なため、実機確認は別途行う。

## 5. 未決事項

- **赤の連鎖インフレ**：`一灯を守る祈り`（連鎖を2＋⌊P÷1,500⌋倍）と GOD `誓いを翔ける天獅子`（2＋⌊P÷300⌋倍）は、HP支払累計を連鎖へ変換する実質的な青×赤の多色核である。段3で多色へ移す選択肢がある。潰す方向では扱わない。ユーザー判断は「赤はあとで調整」。
- **緑の打点変換**：獲得倍率629万倍・エナジー1.8億までインフレするが、獲得倍率を攻撃力へ直接掛けない設計のため最大単発が3色最低になり、完走率も全難易度で最下位。別テーマとして未着手。
- **汎用GODの拡充**：各色3枚しかないため報酬4回で全て揃う。段2と並行して増やさないとランダム性が出ない。マジの汎用（neutral）は現在0枚で、3色で使い回せるため作画効率が最も高い。

## 6. 段2の方向（未着手）

各色の2つ目の核は「主軸 × 副軸」の形を取る。青の例は「連鎖」と「連鎖×ドロー」。青は1ターンに多数のカードを使えるため、手数やドロー数がそのまま倍率になる形が色の性格と一致する。赤・緑の副軸は未定。
